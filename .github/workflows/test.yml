on: [push]

jobs:
  runTest:
    name: test for Jopi
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: node install
        uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: 'https://registry.npmjs.org'

      - name: Installing Npn & Lerna & Yarn
        run: |
          # npm install
          npm install -g npm-cli-login
          echo ${{ secrets.NPM_PASS }}
          # npm install --global lerna
          # yarn install

      # - name: Iniciando Lerna
      #   run: lerna init

      #   - name: run yarn test
      #     run: yarn test

      #   - name: yarn Jest
      #     run: yarn jest --coverage

      - name: Listar tokens
        run: |
          npm-cli-login -u ${{ secrets.NPM_USER }} -p '${{ secrets.NPM_PASS }}' -e ${{ secrets.NPM_EMAIL }}
          npm token
